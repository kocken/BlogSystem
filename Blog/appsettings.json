{
    "ConnectionStrings": {
        // "LocalDB is a lightweight version of the SQL Server Express Database Engine and is intended for application development, not production use"
        // Use "Server = (local)\\SqlExpress;" for (local) production use
        "DefaultConnection": "Server = (localdb)\\mssqllocaldb; Database = MKBlog; Trusted_Connection = True;"
    },
    "Logging": {
        "IncludeScopes": false,
        "LogLevel": {
            "Default": "Warning"
        }
    }
}
